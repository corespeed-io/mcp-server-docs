A Model Context Protocol server that provides web content fetching capabilities, enabling LLMs to retrieve and process content from web pages by converting HTML to markdown for easier consumption, with support for chunked reading via start_index to handle large pages.
